name = "cinenovatv"
main = ".open-next/worker.js"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

assets = { directory = ".open-next/assets", binding = "ASSETS" }

routes = [
  { pattern = "cinenovatv.com/*", zone_name = "cinenovatv.com" },
  { pattern = "www.cinenovatv.com/*", zone_name = "cinenovatv.com" },
]

[vars]
NODE_ENV = "production"
